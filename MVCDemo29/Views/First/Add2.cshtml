
@{
    ViewBag.Title = "Add1";
}

<h2>Add2</h2>

@{ if (ViewBag.res != null)
    {
        <h1> Sum is  @ViewBag.res</h1>
    }
    else
    {
        using (Html.BeginForm("Add2Post", "First", FormMethod.Post))
        {
            @Html.Label("Enter No1 ")
            @Html.TextBox("txtNo1")
            @Html.Label("Enter No2 ")
            @Html.TextBox("txtNo2")
            <button type="submit"> Add</button>
        }
    }

}